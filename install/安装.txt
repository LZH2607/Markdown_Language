安装前，最好断开网络。
安装完成后，解压压缩包x64_Patch.zip，得到文件winmm.dll，将其拖到安装目录下。
打开注册表，按Win和R键，输入regedit。
进入路径：计算机\HKEY_CURRENT_USER\SOFTWARE\Typora
右击左边Typora文件夹，选择“权限(P)...”，弹出“Typora的权限”窗口。
选择“组或用户名(G)”中的每一项，然后将下面的权限（完全控制、读取、特殊权限）全部设为“拒绝”。
破解完成，可连接网络。